{
  "framework": null,
  "buildCommand": "pnpm run build -- --output-path dist",
  "outputDirectory": "dist",
  "installCommand": "pnpm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
